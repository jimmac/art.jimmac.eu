<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
	<generator uri="https://www.getzola.org/">Zola</generator>
	<link href="{{ config.base_url }}/feed.xml" rel="self" type="application/atom+xml" />
	<link href="{{ config.base_url }}/" rel="alternate" type="text/html"/>
	<updated>{{ now() | date(format="%+") }}</updated>
	<id>{{ config.base_url }}/feed.xml</id>
	<title type="html"><![CDATA[{{ config.title }}]]></title>
	<subtitle><![CDATA[{{ config.description }}]]></subtitle>

	{% if config.extra.author_name or config.extra.author_email or config.extra.author_website %}
		<author>
			{% if config.extra.author_name %}
				<name><![CDATA[{{ config.extra.author_name }}]]></name>
			{% endif %}
			{% if config.extra.author_email %}
				<email>{{ config.extra.author_email }}</email>
			{% endif %}
			{% if config.extra.author_website %}
				<uri>{{ config.extra.author_website }}</uri>
			{% endif %}
		</author>
	{% endif %}

	{% if pages %}
	{% for page in pages | slice(end=20) %}
		<entry>
			<title type="html">{{ page.title }}</title>
			<link href="{{ config.base_url }}/{{ page.extra.slug }}/" rel="alternate" type="text/html" title="{{ page.title }}" />
			{% if page.date %}
			<published>{{ page.date | date(format="%+") }}</published>
			<updated>{{ page.date | date(format="%+") }}</updated>
			{% endif %}
			<id>{{ config.base_url }}/{{ page.extra.slug }}/</id>
			<content type="html">
				<![CDATA[<figure><a href="{{ config.base_url }}/{{ page.extra.slug }}/"><img src="{{ config.base_url }}/photos/large/{{ page.extra.filename }}" alt="{{ page.title }}" /></a></figure>]]>
			</content>
			{% if config.extra.author_name %}
				<author>
					{% if config.extra.author_name %}
						<name><![CDATA[{{ config.extra.author_name }}]]></name>
					{% endif %}
					{% if config.extra.author_email %}
						<email>{{ config.extra.author_email }}</email>
					{% endif %}
					{% if config.extra.author_website %}
						<uri>{{ config.extra.author_website }}</uri>
					{% endif %}
				</author>
			{% endif %}
			<media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{ config.base_url }}/photos/large/{{ page.extra.filename }}" />
			<media:content medium="image" url="{{ config.base_url }}/photos/large/{{ page.extra.filename }}" xmlns:media="http://search.yahoo.com/mrss/" />
		</entry>
	{% endfor %}
	{% endif %}
</feed>
